<div class="container mx-auto px-4">
  <h1 class="text-3xl font-bold mb-4 text-center">Edit Password</h1>
  <%= render "form", password: @password %>
  <div class="text-center">
    <%= link_to 'Back', password_path(@password), class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mt-4 inline-block' %>
  </div>
</div>
