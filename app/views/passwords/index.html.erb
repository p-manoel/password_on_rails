<h1>Password on Rails</h1>
<%= link_to "New Password", new_password_path %>

<%= @passwords.each do |password| %>
    <p><%= password.url %></p>
    <p><%= password.username %></p>
    <p><%= password.password %></p>
<% end %>